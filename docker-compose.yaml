services:
  unitycatalog:
    build: .
    command: ["/bin/bash", "bin/start-uc-server", "--port 80"]
  unitycatalog-ui:
    build: ui
  nginx:
    image: nginx:1.27.1
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 8080:80
